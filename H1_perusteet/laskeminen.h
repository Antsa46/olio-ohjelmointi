#ifndef LASKEMINEN_H
#define LASKEMINEN_H

using namespace std;

//Vaihe 2.
void calcSum (int, int);
void calcDiv (int, int);

// Vaihe 3.
int retSum (int, int);
float retDiv (int,int);

#endif // LASKEMINEN_H
